const User = require('./user');
const Account = require('./account');
const Strategy = require('./strategy');
const AdsByStrategy = require('./ads_by_strategy');
const GroupsByStrategy = require('./groups_by_strategy');
const Ad = require('./ad');
const Group = require('./group');

module.exports = {
    User,
    Account,
    Strategy,
    AdsByStrategy,
    GroupsByStrategy,
    Ad,
    Group
};