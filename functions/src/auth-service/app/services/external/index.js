const validateSessionRestClient = require('./restClients/validateSessionRestClient');
const findActiveSession = require('./queries/findActiveSession');

module.exports = {
	validateSessionRestClient,
	findActiveSession
};