rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla temporal para desarrollo - PERMITIR TODO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
